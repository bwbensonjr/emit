## 1. Shared discovery layer

- [x] 1.1 Create `tools/llvm-env.sh`: discover `LLVM_CONFIG` (env → `command -v llvm-config` → `llvm-config-<N>` high→low → known prefixes `/usr/lib/llvm-*/bin`, `/opt/homebrew/opt/llvm@*/bin`, `/usr/local/opt/llvm@*/bin`); fail clearly (platform-neutral) if none found
- [x] 1.2 Derive and export shell-consumer vars: `LLVM_BIN` (`llvm-config --bindir`), `CXX ?= $LLVM_BIN/clang++`, `CXXFLAGS`/`LDFLAGS` (existing `llvm-config` formula); `CC` default = `clang` on PATH else `$LLVM_BIN/clang` (D6)
- [x] 1.3 Discover libgc via `pkg-config bdw-gc` → `GC_INC`/`GC_LIB`, with fallback to prefix `include`/`lib`; derive `GC_DYLIB=$GC_LIB/libgc.<ext>` with `<ext>` from `uname -s` (Darwin=`dylib`, else `so`)
- [x] 1.4 Export `EMIT_LLVM_*` mirrors (`EMIT_LLVM_BIN EMIT_GC_INC EMIT_GC_LIB EMIT_GC_DYLIB`) for the Chez driver
- [x] 1.5 Version floor: parse `llvm-config --version` major; `say` a warning (not fail) if below the documented floor (default **19**, overridable via `EMIT_LLVM_MIN`); no upper bound
- [x] 1.6 Narrate discovered LLVM bin dir, version, and libgc location via `tools/log.sh` (`say`/`vsay`, stderr) honoring `EMIT_VERBOSITY`; add a `--print-make` mode emitting Make-syntax `VAR := value`

## 2. Wire shell + Make consumers

- [x] 2.1 `Makefile:29-37`: replace the hardcoded block with `-include build/llvm.mk` generated by `tools/llvm-env.sh --print-make`; keep env/command-line overridability; leave downstream recipes unchanged
- [x] 2.2 `bin/scheme-compile:21-24`: replace `${VAR:-/opt/homebrew…}` defaults with `. tools/llvm-env.sh`
- [x] 2.3 `tools/regen.sh:30-35`: same source-the-layer swap
- [x] 2.4 `test/self-host-fixpoint.sh:31-35`: same source-the-layer swap
- [x] 2.5 Update stale comments in `run-all-tests.sh:14` and `test/module-scaffold-baseline.sh:22` to "needs an LLVM discoverable via llvm-config + libgc"

## 3. Chez driver (`src/compile.ss`)

- [x] 3.1 `gc-inc`/`gc-lib` (45-46): `getenv EMIT_GC_INC`/`EMIT_GC_LIB` → else `pkg-config bdw-gc` self-probe → else legacy default
- [x] 3.2 `llvm-bin` (157): `getenv EMIT_LLVM_BIN` → else `llvm-config --bindir` self-probe → else legacy default
- [x] 3.3 `gc-dylib` (159): `getenv EMIT_GC_DYLIB` → else `<gc-lib>/libgc.<ext>` with `<ext>` by platform (or probe which file exists) — fixes the Linux JIT bug
- [x] 3.4 AOT clang at target-header probe (54) and `link` (174): resolve as `EMIT_LLVM`/env override → `clang` on PATH → discovered `$LLVM_BIN/clang` (D6), instead of a hard bare `clang`
- [x] 3.5 `require-llvm-tools` (163-170): probe discovered tools; on miss error with a platform-neutral message naming `apt`/`brew` options and the `EMIT_LLVM_BIN` override; optionally add the version-floor warning
- [x] 3.6 Verify the modular jit/bitcode backends (~405-438, 468-469) inherit the fixes via the shared `tool`/`gc-dylib` helpers

## 4. Hardcoded test scripts

- [x] 4.1 `test/repl.ss:27-28`: route `gc-inc`/`gc-lib` through the env vars / discovery
- [x] 4.2 `test/repl-batch-tests.sh:21`: replace bare `clang -I/opt/homebrew…` with the sourced-layer values

## 5. Docs

- [x] 5.1 `README.md:13,24`: "recent LLVM discovered via llvm-config + libgc"; give both `brew` and `apt`/`libgc-dev` install lines; fix the "libgcz" typo
- [x] 5.2 `src/TOOLCHAIN.md` and `LLVM.md:88-97,373-378`: document discovery, `LLVM_CONFIG`/`EMIT_LLVM_*` overrides, the soft version floor, and platform shared-lib extension; drop "pinned keg off PATH" as the only story

## 6. Verification

- [x] 6.1 `tools/llvm-env.sh --print-make` on the Linux/apt box prints `/usr/lib/llvm-22`-based paths and `libgc.so`; sourcing exports `EMIT_LLVM_*`
- [x] 6.2 `make clean && make all schemec build/scheme-run` links against the discovered LLVM with no path edits
- [x] 6.3 AOT: `bin/scheme-compile demos/fact.scm -o /tmp/fact && /tmp/fact` (proves discovered clang)
- [x] 6.4 `demos/run-backends.sh` reports AOT|JIT|bitcode identical across the suite (proves the `.so` JIT fix + Chez-driver discovery)
- [x] 6.5 `EMIT_LLVM_BIN=/nonexistent bin/scheme-compile demos/fact.scm` fails with a clear, platform-neutral message naming the override
- [x] 6.6 `make regen` converges (fixed point) using discovered tools
- [x] 6.7 Read-verify the macOS branches: search list still finds `/opt/homebrew/opt/llvm@*/bin`, `.dylib` retained on Darwin
